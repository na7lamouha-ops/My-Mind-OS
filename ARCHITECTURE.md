# ARCHITECTURE — My Mind OS

## نظرة عامة

تطبيق Next.js 14 (App Router) بلغة TypeScript، واجهة عربية RTL بثيم داكن، وقاعدة بيانات
Supabase (Postgres + Auth) محميّة بالكامل بـ RLS. مستخدم واحد فقط (شخصي).

```
المتصفح ──▶ Next.js (Server Components + Route Handlers) ──▶ Supabase (Postgres/Auth)
                         │
                         └── كل استدعاء AI يتم هنا على الخادم (لاحقًا) — لا من المتصفح
```

## الطبقات

| الطبقة         | الموقع                   | المسؤولية                                     |
| -------------- | ------------------------ | --------------------------------------------- |
| العرض          | `src/app/**`             | صفحات App Router، Server Components افتراضيًا |
| المكوّنات      | `src/components/**`      | عناصر واجهة قابلة لإعادة الاستخدام            |
| المخططات       | `src/schemas/index.ts`   | Zod: مصدر الحقيقة للأنواع والتحقق             |
| Supabase       | `src/lib/supabase/**`    | عملاء المتصفح/الخادم + تحديث الجلسة           |
| البيئة         | `src/lib/env.ts`         | تحقّق Zod من متغيرات البيئة، فصل عام/خادم     |
| الحماية        | `middleware.ts`          | تحديث الجلسة وحماية مسارات `/dashboard`       |
| قاعدة البيانات | `supabase/migrations/**` | الجداول، الفهارس، المشغّلات، RLS              |

## نموذج البيانات (8 جداول)

جميع الجداول تشترك في: `id`, `owner_id`, `created_at`, `updated_at`, `deleted_at`
(حذف ناعم).

```
users ──< projects ──< tasks
                │  └─< ideas ─┐
                │             ├─< content_items
                └─< sources ──┘
links (ربط عام بين أي كيانين)
ai_runs (اقتراحات AI بانتظار موافقة بشرية)
```

- **فكرة** تُربط اختياريًا بمشروع.
- **مصدر** يُربط اختياريًا بمشروع و/أو فكرة.
- **مهمة** تنتمي إلزاميًا إلى مشروع (`project_id NOT NULL`).
- **محتوى** ينبع اختياريًا من فكرة و/أو مصدر.
- **links** جدول ربط عام (from_type/from_id ↔ to_type/to_id) لأي علاقات إضافية.
- **ai_runs** يسجّل كل تشغيل AI مع `status` بشري (pending → proposed → approved/rejected).

## قواعد منع التشتت (مطبّقة في قاعدة البيانات)

- **مشروع نشط واحد فقط**: فهرس فريد جزئي `projects_one_active_per_owner`.
- **الفكرة لا تتحوّل لمشروع تلقائيًا**: لا مشغّل يفعل ذلك؛ الربط يدوي عبر `project_id`.
- **الحذف = أرشفة**: عمود `deleted_at` بدل الحذف الصلب (Archive / Snoozed).

## نموذج الأمان

- **RLS على كل جدول**: `owner_id = auth.uid()` للقراءة والكتابة (جدول `users` عبر `id`).
- **إنشاء صف المستخدم** يتم بمشغّل `security definer` عند إنشاء مستخدم Auth.
- **الأسرار**: القيم العامة فقط تبدأ بـ `NEXT_PUBLIC_`. مفاتيح الخدمة تُقرأ على الخادم فقط
  عبر `getServerEnv()` ولا تدخل حزمة المتصفح.
- **AI عبر الخادم**: تُنفَّذ استدعاءات المزوّد داخل Route Handlers فقط (لاحقًا).

## الاختبارات

- **Vitest** (`tests/schemas.test.ts`): تحقّق مخططات Zod للكيانات الثمانية.
- **Playwright** (`tests/e2e/home.spec.ts`): المسار الأساسي — الصفحة الرئيسية → لوحة التحكم.

## بوابة الموافقة البشرية (Human Approval Gate)

قبل أي إجراء حسّاس (حذف نهائي، تغيير Schema، نشر، استدعاء AI خارجي) يجب التوقّف وطلب
موافقة صريحة. حالة `ai_runs.status` تجسّد هذه البوابة على مستوى البيانات.
